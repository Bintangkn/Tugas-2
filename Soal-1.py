kontak = []

def daftar():
    for x in kontak:
        print("nama: {}" .format(x["nama"]))
        print("no. telepon: {}" .format(x["no. telepon"]))

def tambah():
    x = {}
    nama = input("nama: ")
    no.telepon = input("no. telepon")
    x["nama"]=nama
    x["no. telepon"] = telepon